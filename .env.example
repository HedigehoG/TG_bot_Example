# Этот файл — пример. Скопируйте его в .env и заполните своими данными.
# Файл .env не должен попадать в Git.

BOT_TOKEN=123456:ABC-YOUR-REAL-TOKEN-HERE
WEBHOOK_HOST=https://your-domain.com
# PORT - это ВНУТРЕННИЙ порт, который слушает приложение в контейнере.
# Внешний порт настраивается в docker-compose файлах (например, 8001:8080, где 8001 - внешний, 8080 - внутренний).
PORT=8080