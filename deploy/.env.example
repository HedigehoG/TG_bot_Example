# Этот файл — пример. Он создается автоматически при запуске bootstrap-server-custom.sh
# Файл .env не должен попадать в Git.

# Передается через секреты GitHub
BOT_TOKEN=123456:ABC-YOUR-REAL-TOKEN-HERE
# WEBHOOK_HOST должен указывать на публичный адрес вашего сервера, например https://my-first-bot.your-domain.com
WEBHOOK_HOST=https://your-domain.com
